	/*
			 * 上午:
			 * 权限修饰符号:
			 * public
			 * private:
			 * 
一. Student  student ;
 这是在定义,是在声明
 student 是引用
 
 Student s = new Student();
 student = s;
 这是引用之间,传递对象的地址值;
  ---------------------------------------
 二: 
 public void show(String a){}
				这里的a也是一个引用
			在调用show方法的时候,会传递参数到括号中去,
 				这个参数就会被a引用 
				
 
 三:
 private :  共有的 表示在哪里 都可以调用这个方法
 public 		私有的,表示只能在本类中使用
 protected	受保护的, 同包下,或则子类 可以使用
				什么都不写,表示默认的,在同包下可以使用



四:
什么是包
英文:package
一个班级中,存在一个叫张三的同学,
 现在又来了一个叫张三的同学怎么办?
 =
 注意:*/*包的存在:就是区分同名的类
 
 包的命名 : 公司的网址倒置. 这样能保证,不同公司开发的类,即使同名了也没有关系 比如在蓝鸥公司学习的
: com.lanou3g			  



五:
总结方法的组成
1.权限修饰符,表示这个方法可以在哪里执行
2.返回值类型: 表示这个方法执行的结果是什么类型的

 如果这个类型是void 型的,表示这个方法执行的结果不需要告诉调用者
  
3.方法名:是一个方法的标识
4.参数列表:::
 执行这个方法的必要条件就用应用参数列表表示stu.show("哈哈哈");这里就是参数列表为一个字串符
5.方法体:方法被调用时,真正执行的代码		

六:
权限修饰符:也可以修饰属性:
生成get / set 方法的快捷键
alt + shift + s	